# 진화 루트(랜덤 진화) 요구사항

2026-07-30 정리. `lib/daily.js`의 `selectRoute`와 `sprites/packs/*/pack.json`의 `tree`가
대상이다. 구현된 부분과 아직 아닌 부분을 분리해 적었다.

## 1. 왜 고치는가

첫 구현(커밋 `2e52e20`)은 단계별로 후보를 뽑고, 후속이 없는 분기를 만나면 **다음 단계의
spine으로 되돌아왔다**(spine 복귀). 이게 그래프에 없는 엣지를 코드가 만들어내는 짓이었다:

- `라이즈그레이몬 → 워그레이몬` — 실제 후속은 샤인그레이몬이고, 워그레이몬은 메탈그레이몬
  계보다. 서로 다른 두 계보를 이어붙였다.
- `블랙메가로그라우몬 → 듀크몬`, `매그너몬 → 황제드라몬 파이터 모드` — 같은 방식의 가짜 엣지.

결과적으로 "성숙기에서 갈라지면 이후 진화체도 제약된다"는 디지몬 진화의 기본 성질이 깨졌다.
표기상 14개였던 하루 루트 중 절반이 이렇게 만들어진 것이었다.

별개로, 게임 표에는 있지만 계보 감각과 어긋나는 엣지도 있다:

- `워가루몬 → 다크드라몬` (DWDS 표에 존재). 통상은 **블랙워가루몬 → 다크드라몬**이다.
- `인퍼몬 → 베르제브몬` (DWDS 표에 존재). 캐논상 베르제브몬은 임프몬 계보다.

## 2. 설계 원칙

- **인접리스트가 유일한 진실.** 노드의 `next`에 없는 전이는 존재하지 않는다. 폴백으로
  전이를 합성하지 않는다.
- **크기에 맞는 도구.** 전체 그래프는 노드 수십 개 규모다. graph DB·전용 엔진은 과잉이며,
  `pack.json`의 인접리스트 + 20줄 DAG 워크로 충분하다.
- **부분 완성이 정상 상태.** 트리는 매주 조금씩 채워진다. 반쯤 만든 분기가 그날의 추첨을
  깨뜨리지 않아야 한다.
- **없는 데이터를 그럴듯하게 만들지 않는다.** 도트가 없으면 그 분기는 쓰지 않는다. 지표가
  일 단위가 아니면 그렇다고 적는다.

## 3. 데이터 모델 (`pack.json`)

```jsonc
"tree": {
  "<stageId>": [
    {
      "id": "greymon",          // 팩 안에서 유일
      "name": "그레이몬",        // 표시 이름 (한글명 규칙은 §7)
      "sprite": "adult",        // 도트 파일 prefix: <sprite>-0.png, -1.png ...
      "next": ["metalgreymon"], // 같은 팩의 노드 id, 반드시 다음 스테이지
      "traits": ["focus"]       // 선택. §5의 trait 편향에 쓰임
    }
  ]
}
```

- 각 스테이지 배열의 **첫 노드가 spine**이다. 트리가 없는 팩은 `stageNames`로 동작한다(호환).
- `sprite`가 spine이면 관례적으로 스테이지 id와 같다(`adult-0.png`). 분기는
  `adult-geogreymon-0.png`처럼 `<stage>-<id>` 규칙을 쓴다.
- 도트 파일은 커밋되지 않는다. 따라서 **트리에 이름이 있어도 도트가 없을 수 있다**는 것이
  정상이며, 선택 로직이 이를 걸러야 한다(R2).

## 4. 선택 로직 요구사항

| ID | 요구사항 | 상태 |
|---|---|---|
| R1 | 전이는 `next`에 있는 것만. spine 복귀 등 합성 엣지 금지 | ❌ 제거 필요 |
| R2 | 후보 조건 = **최상위 스테이지까지 엣지가 이어짐** AND **경로상 모든 노드의 `sprite` 도트가 존재** | ❌ 신규 |
| R3 | 하루 고정: `hashString(dateKST\|팩\|스테이지)` 기반, 같은 날 재실행 시 동일. `daily.json`에 오늘 루트가 있으면 재사용 | ✅ 구현됨 |
| R4 | 지표 편향: 그날 활성 trait과 겹치는 후보가 있으면 그 안에서만 추첨, 없으면 전체 후보 | ✅ 구현됨 |
| R5 | 어제와 동일한 루트면 마지막 분기 스테이지를 한 칸 민다 | ✅ 구현됨 |
| R6 | 루트는 항상 최상위 스테이지에 도달한다 (R2의 귀결) | ⚠️ 가짜 엣지로 달성 중 → R2로 대체 |
| R7 | 후보가 없으면(도트 미비 등) 트리를 무시하고 `stageNames` 경로로 폴백 | ❌ 신규 |
| R8 | `Math.random` 사용 금지(멱등성) | ✅ 구현됨 |

R2의 판정은 팩 로드 시점에 한 번 계산한다(스테이지 역순으로 도달 가능성 전파 —
최상위부터 내려오며 "도달 가능 = 도트 있음 AND (최상위이거나 도달 가능한 후속이 하나 이상)").

## 5. 성장 배경(trait) — 현재값과 한계

| trait | 조건 | 데이터 출처 | 한계 |
|---|---|---|---|
| `dark` | 도구 호출 실패율 ≥ 5% | `global.json` 누적 카운터 | **일 단위가 아님.** 일일 실패 카운터를 `hook.js`에 추가해야 해결 |
| `swarm` | 그날 토큰을 쓴 세션 ≥ 5 | `daily.json` `sessionTokens` | 일 단위 ✅ |
| `focus` | 한 세션이 그날 output의 60% 이상 | 동일 | 일 단위 ✅ |

## 6. 점진적 채우기 (주 ~15노드)

R2가 있으면 다음 순서로 아무 때나 노드를 추가할 수 있다.

1. DWDS 시트를 `sprites/sheets/dwds/<name>.png`로 넣는다.
2. `python3 scripts/extract_pack_evolved_dwds.py --contact <name> /tmp/x.png` → 정면 걷기
   프레임 2개를 눈으로 고른다.
3. `PICKS`에 한 줄(`"<stage>-<id>": ("<sheet>", [i, j])`), `pack.json` `tree`에 노드 하나.
4. 추출 → `node --test`.

노드당 1~2분이며 병목은 프레임 고르기다. **최상위까지의 사슬을 완성시키는 노드를 우선**한다
(한 노드가 루트 하나를 즉시 늘린다). 사슬 중간 노드만 늘리면 R2 때문에 아직 추첨에 들어오지
않는다 — 의도된 동작이다.

### 필요한 도구

| 도구 | 목적 | 상태 |
|---|---|---|
| `--contact <sheet>` | 감지된 필드 프레임을 번호 붙여 한 줄로 렌더 | ✅ 있음 |
| 트리 lint 테스트 | `next` id 존재, 스테이지 순서 위반·순환 없음, spine 존재 | ❌ 신규 |
| `scripts/tree-status.js` | 도트 없는 노드 / 후속 없는 노드 / 추첨 제외 사유 / 필요한 시트 목록 | ❌ 신규 |

## 7. 이름 규칙

한글명은 [Wikimon](https://wikimon.net)의 「Korean (한국어)」 표기를 따른다. 직역과 다른
사례가 실제로 여럿 있었다: GeoGreymon = 지오그레이몬, Peckmon = 펙크몬, Varodurumon =
발두르몬, Armagemon = 아마게몬, Flamedramon = 화염드라몬, Magnamon = 매그너몬,
Imperialdramon = 황제드라몬. 한국 위키 사이트(나무위키·우만위키)는 봇 차단이라 자동 조회
경로로 쓸 수 없다.

## 8. 결정 기록

| # | 결정 | 근거 |
|---|---|---|
| D1 | spine 복귀 제거, R2(도달 가능성 + 도트 존재) 도입 | 가짜 엣지가 계보 제약을 깼다 |
| D2 | 최상위 미달 루트는 추첨하지 않는다(B안) | 2M 도달일에 초궁극체를 못 보는 일이 없어야 한다 |
| D3 | 지연 진화(단계 도달 시점에 다음 노드 결정, A안)는 보류 | D2와 상충. `dark`를 일 단위로 만들면 이점 일부를 따로 얻을 수 있다 |
| D4 | `다크드라몬`은 `가루몬 → 블랙워가루몬 → 다크드라몬`으로 재배치 | 게임 표의 `워가루몬 → 다크드라몬`이 계보 감각과 어긋난다. 시트 존재(`BlackWereGarurumon` 48678) |
| D5 | 쓰지 않는 분기 도트는 지우지 않는다 | gitignore 대상이고, 상위 노드가 채워지면 즉시 되살아난다 |

## 9. 열린 질문

- **Q1.** `인퍼몬 → 베르제브몬`을 유지할까? DWDS 표에는 있지만 캐논상 임프몬 계보다.
  유지 시 케라몬 루트 2개, 제거 시 1개.
- **Q2.** D2를 유지하면 남은 백로그 대부분(§10)이 최상위에 닿지 못해 추첨에 영원히
  들어오지 않는다. 주 15노드를 계속 채우려면 셋 중 하나가 필요하다:
  (a) 후보 조건을 "궁극체 도달"로 낮춤, (b) A안(지연 진화)으로 전환해 미달 루트 허용,
  (c) 초궁극체 도트를 다른 출처에서 확보(Lost Evolution 립 등).
- **Q3.** 블랙워그레이몬 도트는 감지된 필드 행이 뒷모습이다. 다른 행을 잡는 방법을 찾을지,
  현재 도트로 둘지.

## 10. 백로그 (DWDS 시트 보유 = 즉시 작업 가능)

| 팩 | 추가 노드 | 시트 | 루트 증가 |
|---|---|---|---|
| 파피몬 | 블랙워가루몬 (D4) | ✅ 48678 | +0 (기존 다크드라몬 루트를 정상화) |
| 길몬 | 카오스듀크몬 | ✅ 48621 | +0 (궁극체까지만 — Q2) |
| 테리어몬 | 블랙라피드몬, 프레리몬, 세이버레오몬, 블랙메가가르고몬 | ✅ 48570/48692/48454/48706 | Q2 해결 시 +2 |
| 임프몬 | 로치몬, 바이럴몬, 소서리몬, 메가쿠와가몬 블루, 미미몬, 크리피몬, 구울몬 | ✅ 전부 | Q2 해결 시 +3 |
| 레나몬 | 쿠즈하몬 | ✅ 48778 | Q2 해결 시 +1 |
| 아구몬 | 샤인그레이몬 | ❌ DS 립 없음 | 지오그레이몬 라인 대기 |

## 11. 현재 상태 (2026-07-30)

- 브랜치 `feat/superultimate-stage`, PR #13 **미머지**(`menubar/claudemon-menubar.swift`가
  `feat/portrait-cutin`의 초상 작업과 겹침). 최신 커밋 `2e52e20`.
- 로테이션 후보 6팩: 아구몬·파피몬·길몬·브이몬·케라몬·팔코몬. 초궁극체 미보유 3팩(레나몬·
  테리어몬·임프몬)은 제외 상태.
- 표기상 하루 루트 14개. R1/R2 적용 후 9개(+D4로 정상화)로 줄어든다.
- 머지 후 메뉴바 재빌드 필요: `cd menubar && swiftc -O -o claudemon-menubar
  claudemon-menubar.swift` → `launchctl kickstart -k gui/$(id -u)/com.muo.claudemon-menubar`.
