# TODOS

의도적으로 미룬 작업. 각 항목은 3개월 뒤에 읽어도 동기와 시작점이 복원되도록 적는다.

> **우선순위 (2026-08-06)**: 3·4번 variation 확장 트랙을 1순위로 승격, 착수. 1·2번은 후순위.
> 착수 시점 실사 결과: §10 백로그 중 블랙워가루몬·블랙라피드몬·블랙세인트가르고몬·쿠즈하몬은
> 이미 트리에 편입돼 있었다(§10이 2026-07-30 기준이라 낡았음 — 당일 실사로 갱신 완료).
>
> **우선순위 전환 (2026-08-20, Phase B 전환 후)**: Phase B(전역 진화 그래프) 완료로 병목이
> "도트 발굴"에서 "정사 대조 + 조건 축"으로 이동했다. 2026-08-06 우선순위의 전제("도트 발굴이
> 유일한 병목")가 무너졌다. 실측(2026-08-20): 노드 79 · 엣지 93 · 조건부 16(17%) · 분기점 17 ·
> **fan-out=1인 노드 50개**. 궁극체 칸은 노드 17개 중 분기점 3 · fan-out=1 13개 — 궁극체에
> 닿은 뒤로는 진화가 거의 결정론이다. 새 우선순위:
> 1. **엣지 발굴 (도트 0장)**: 두 노드가 이미 있고 정사 엣지가 미실현인 자리. 분기점 단가가
>    "도트 4장 + 노드"에서 "엣지 한 줄"로 떨어졌다. `scripts/audit-canon-edges.js`가 대조를
>    자동화한다. 조회 방향은 **자식의 `Evolves From`** (§B-6: 부모의 `Evolves To`는 조그레스
>    파트너 오탐이 절반이었다).
> 2. **조건 축**: 엣지가 싸지면서 `docs/global-graph-plan.md` §7의 상한(하루 안에서 의미 있게
>    움직이는 조건 축 5개)이 예상보다 빨리 다가온다. fan-out이 축보다 많아지면 타이브레이크가
>    날짜 해시 = 제비뽑기로 떨어져 §0 목표("모든 진화 분기가 그 순간의 조건으로 결정")와 어긋난다.
>    편입 시 그 부모의 파생 fan-out을 함께 기록하고, 게이트에 조건 축 계획을 추가한다.
> 3. **도트 립 (대상 선정 기준 뒤집힘)**: 한 팩의 라인을 초궁극체까지 완성하는 것이 로테이션
>    자격이었으나, D7(종점 인정) + Phase B(종점 파생)로 그 제약이 사라졌다. 지금 노드 하나의
>    값은 **몇 개 라인이 그것을 재사용할 수 있는가**로 정해진다(§B-4 티라노몬 예시 참고).
>
> **1차 편입 완료 (2026-08-06)**: 임프몬 다크 라인 이블몬→미이라몬→데스몬→데스몬 블랙
> (+1 루트, 게이트 sessionCount ≥ 5). DWDS 시트 4장 신규 확보(Spriters Resource
> 48620/48669/48818/48820), 도트·초상 추출, 트리 편입, `node --test` 70/70 ·
> `sprite_status.py --check` 통과. 상세는 `docs/evolution-routes.md` §10.
>
> **2차 편입 완료 (2026-08-06, PR #18)**: Q2 재검 결과 상당수는 시트 문제가 아니라
> **계보 확인 문제**였다 — Wikimon "Evolves To" 전수 재검으로 길몬 카오스 라인(+1),
> 브이몬 아머체 2종(+2), 테리어몬 프라리몬(+1, 시트 1장 신규)이 기존 에셋으로 해소됐다.
> 하루 루트 합계(전 팩, 맵 진단 기준): 오늘 아침 19 → 1·2차 후 26. 남은 것은 진짜
> 막힌 것뿐(§10 잔여 표): 타이란트캅테리몬 도트,
> 가오몬 BM 대체 시트, 레나몬 분기 소재 — 셋 다 외부 립 발굴 대기. 소서리몬·크리피몬·
> 세이버레오몬은 정사 접점 부재로 사실상 영구 제외.
>
> ~~**유지보수 모드 전환 (2026-08-07)**: 3·4번의 잔여분은 도트 발굴이 유일한 병목이라
> **주간 발굴 routine**을 걸었다(스케줄드 에이전트). 찾는 것 3건: ① 타이란트캅테리몬
> 워킹 도트 ② 미라지가오가몬 BM 무워터마크 시트(Dawn/Dusk 립) ③ 레나몬 분기 소재
> (사쿠야몬 미코 모드 등). 발굴 성공 시 이 파일과 §10 잔여 표를 갱신하고 편입은
> 사람이 프레임을 검수한 뒤 진행한다(인덱스 추측 금지 원칙 유지).~~
>
> **도트 발굴 병목 해소 (2026-08-20)**: 위 전제("도트 발굴이 유일한 병목")가 무너졌다.
> Wikimon이 종명 기반으로 도트를 호스팅한다는 것이 실측으로 확인됐다 — ROM 인덱스를
> 추측할 필요 없이, 종의 Wikimon 페이지 제목만 있으면 `scripts/fetch-wikimon-dots.py`가
> MediaWiki API로 실제 파일명·존재 여부를 조회해 내려받는다. 확보 가능한 풀은
> **Digimon Story 오버월드(`_dst_map.png`/`_dsle_map.png`) 337종** + **컬러 vpet 도트
> 세트 848종**(`source: "vpet_*"`, 배경 제거·스케일 보정까지 자동화 완료) 두 겹이다.
> `scripts/harvest-wikimon-canon.js`(정사 Evolves From + 출처 게임 판정, `--roster-mode
> relaxed`로 신규 편입분만 로스터 게이트 완화 — `docs/evolution-routes.md` §9 Q5)와
> 짝을 이뤄, 이번에 **235종이 그래프에 편입됐다**(79 → 314 노드, `docs/graph-provenance.json`에
> 각 엣지의 출처 게임과 strict 게이트 통과 여부 기록). 남은 확장 여지: **vpet 경로는
> 구현은 끝났지만 아직 쓰지 않았다** — 337종 오버월드 풀만으로 235종을 채웠고, 848종
> vpet 풀은 다음 편입 라운드가 오버월드 풀을 다 쓴 뒤에야 필요해질 여유분이다.

---

> 맥락: 조건부 진화 엣지는 2026-08-05 `feat/conditional-evolutions` 7커밋으로 구현 완료.
> 분기 선택이 날짜 해시에서 조건 게이트(`evolutions[].when`)로 바뀌었고, 도달 보장은
> "비최상위 노드의 마지막 엣지 = `when: null`" 데이터 계약(`validatePackTree`)이 됐다
> (규칙·근거는 README §랜덤 진화와 해당 커밋 메시지들). 1·2번은 그 설계 리뷰에서 범위
> 밖으로 밀어낸 항목, 3·4번은 후속 variation 확장 트랙이다.

---

## 1. 스테이지 스킵 검증을 팩 스키마 계약으로 승격 — ✅ 완료 (2026-08-07)

**완료**: `docs/evolution-routes.md` §4 에 R9(스테이지 스킵 금지)로 명문화했다.
아래 Why 의 실패 시나리오·공개 계약 근거를 R9 본문에 그대로 옮겼고, §4 상태 열이
2026-07-30 기준이라는 주석과 조건부 진화 전환 이후의 R1/R2/R6 상태도 함께 달았다.

**What** — `evolutions[].to` 가 *바로 다음 선언 스테이지*의 노드를 가리켜야 한다는 규칙을
`docs/evolution-routes.md` 의 루트 요구사항으로 명문화한다.

**Why** — 이 제약은 지금 구현 세부에만 존재한다. `lib/daily.js` 의 `byId` 는 전 스테이지
노드를 한 맵에 담고, 후보 필터에 스테이지 검사가 없다. 그래서 성숙기 노드가 궁극체 id를
가리키면 그 노드가 **완전체 슬롯에 배정된다.** 스프라이트 이름 조회가 어긋나 메뉴바 앱이
한 단계 아래 프레임으로 폴백하고(README §스프라이트 폴백), 사용자에게는 "진화했는데
퇴화한 것처럼" 보인다 — README 가 과거 버그로 기록해둔 바로 그 증상이다.
README 는 커스텀 팩 제작을 공개 계약으로 약속하는데, 제작자가 이 제약을 알 방법이 없다.

**Pros** — 커스텀 팩 생태계에서 사일런트 실패를 예방한다.
**Cons** — 조건부 엣지 작업의 `validatePackTree` 가 실용적으로는 이미 잡는다. 남은 건
대부분 문서 작업이다.

**Depends on** — ~~조건부 엣지 변경의 `validatePackTree` 완료 후~~ → 충족됨 (2026-08-05,
`validatePackTree` 가 `node --test` 전수 테스트로 스테이지 스킵을 잡는다. README
§랜덤 진화에도 규칙이 실렸다). 남은 것은 `docs/evolution-routes.md` 루트 요구사항
명문화뿐.

---

## 2. 퇴화(`TREE.regression`) 활성화 검토 — ⏸ 무기한 보류 (2026-08-07)

**보류**: 사용자 결정으로 아예 pending 처리. 능동적으로 재개하지 않는다 — 사용자가
명시적으로 다시 열 때만 착수하고, 그때 아래 Cons 의 설계 결정 3건(래치 vs 되돌림,
statusline 부수효과, KST 자정 리셋과의 중복)부터 토론한다.

**What** — `evolution-tree.json` 에 `regression` 키를 넣어 `applyRegression` 을 살릴지
결정한다.

**Why** — `claudemon-prd.md` §2 의 명시 요구사항이다("방치 또는 나쁜 습관에 따라 조건
저하 → 퇴화 가능"). 코드는 이미 있다 — `lib/evolve.js:66 applyRegression` 이 마지막 활동
시각으로부터 N시간 경과 시 지정 스테이지로 되돌린다. 그런데 `evolution-tree.json` 에
`regression` 키가 없어서 `if (!rule) return false` 로 즉시 반환하는 **죽은 경로**다.

**Pros** — PRD 완결. 방치에 대한 피드백 루프가 생긴다.

**Cons** — 세 가지가 걸린다.
1. `locked` lazy binding(화면 무깜빡임 보장, `lib/daily.js:211-219`)과 정면 충돌한다.
   "조건이 거짓으로 돌아가면 되돌아가나, 아니면 래치인가"를 먼저 정해야 한다.
2. `statusline.js:45` 가 이미 `applyRegression` 을 호출한다. 데이터를 넣는 순간
   statusline 동작이 바뀐다 — 조용한 활성화가 아니다.
3. 하루 KST 리셋 모델에서는 자정 리셋이 이미 사실상의 퇴화다. 중복일 수 있다.

**Context** — 일일 리셋은 의도된 동기 설계라는 결정이 이미 내려져 있다(2026-08-05,
Approach C 기각 근거). 퇴화를 켤 때 그 결정과 충돌하지 않는지 함께 봐야 한다.

**Depends on** — 없음. 단 조건부 엣지 변경 이후가 안전하다.

---

## 3. Variation 확장 트랙 1 — 백로그 도트 추출·편입 (계약 비충돌분)

**What** — `docs/evolution-routes.md` §10 백로그(DWDS 시트 확보 완료 ✅)를 도트로
추출해 팩 트리에 `evolutions` 노드로 편입한다. 순서:

1. 시트별로 `python3 scripts/extract_pack_evolved_dwds.py --contact <sheet>` 를 돌려
   번호 스트립을 **눈으로 보고** 정면 포즈 프레임 인덱스 2개를 고른다.
   **인덱스 추측 금지** — 재실행이 현재 파일을 덮어쓰고 되돌릴 근거가 없다
   (스크립트 주석의 명시 경고. `PICKS` 에 등록된 것만 추출된다).
2. `PICKS` 등록 → 추출 → `docs/sprite-aliases.yaml` / `sprite_status.py --check` 정합.
3. `pack.json` 에 조건 엣지로 편입. **비최상위 노드의 마지막 엣지 = `when: null`
   계약**(README §랜덤 진화)을 지켜야 하며, `node --test` 의 출하 팩 전수 검증이 안전망이다.

**Why** — 조건부 진화 게이트는 깔렸는데 정작 분기가 있는 팩이 10개 중 6개뿐이다
(길몬·임프몬·레나몬·가오몬은 단일 루트 — 조건이 걸릴 자리가 없다). 조건 게이트의
체감 가치는 분기 수에 비례한다.

**주의** — D7(2026-08-19) + Phase B(2026-08-20)로 최상위 도달 제약이 사라졌다. 지금
편입 기준은 **라인 재사용 폭**이다: 그 노드를 몇 개 라인이 경유할 수 있는가(부모 in-degree +
자식 fan-out으로 추정). 백로그 노드 다수(테리어몬 블랙라피드몬 계열, 임프몬 +7, 레나몬
쿠즈하몬)는 Phase B 이전에는 종점 미달로 탈락했으나, 이제 궁극체 종점도 로테이션 대상이다.
우선순위는 **다음 칸 복수 노드가 부모로 지목할 수 있는 자리**(티라노몬 등 §B-4 실측 예시)부터.

**Depends on** — 없음 (사람이 인덱스를 고르는 수작업이 병목).

---

## 4. Variation 확장 트랙 2 — Q2 해소: 궁극체에서 멈추는 정사 라인 되살리기 — ✅ (a)안 채택·D7 구현 (2026-08-19)

**What** — 정사상 초궁극체가 없어 걷는 트리에서 빠진 라인을 되살릴 방법을 결정한다.
대상: 길몬 블랙메가로그라우몬→카오스듀크몬, 브이몬 화염드라몬·매그너몬(아머체),
그리고 3번 백로그 중 궁극체에서 끝나는 노드 전부. 제외 이력과 재편입 대상은
`docs/sprite-aliases.yaml` `pack_sprite_to_catalog` 의 2026-08-05 주석에 있다.

**결정 (D7, `docs/evolution-routes.md` §8/§9 Q2)** — **(a)안 채택.** 후보 조건을
"궁극체 도달"로 낮추되, 2026-08-05에 락인한 결정 C("마지막 엣지 = `when: null`")를
그대로 여는 대신 종점 노드에 `terminal: true`를 명시하는 데이터 계약을 얹었다.
이전에 이 문서가 (a)안을 "비추천"으로 적었던 이유("2M 찍은 날 초궁극체를 못 보는"
회귀)는 화면에서 `stageId`를 종점 스테이지 위로 clamp하고 `terminalFrom`으로
"어디서 멈췄는지"를 알리는 것으로 해소했다 — 라인이 끝난 자리를 사용자가 그대로
보되, 그 자리가 무엇인지 명시된다. (c)(대체 도트 확보)는 이 결정과 독립된 트랙으로
아래 재등록한다. (b)(지연 진화 A안)는 D3에서 이미 보류 상태로 남는다.

**Why** — 3번만으로는 게임 기준 variation 의 절반이 잠긴 채로 남는다. 백로그 표의
"Q2 해결 시 +N" 루트 증가분(테리어몬 +2, 임프몬 +3, 레나몬 +1)이 전부 여기 묶여 있다.
D7 자체는 가오몬 궁극체 노드 하나(`terminal: true`)만 편입해 로테이션 복귀를
확인했다 — 나머지 8라인(테리어몬·임프몬·레나몬 등)의 실제 편입은 3번 트랙의 도트
추출이 선행돼야 하는 별도 작업으로 남는다.

**Depends on** — 3번과 독립이나, 실제 데이터 편입 시 3번의 추출 파이프라인을 그대로 쓴다.

---

## 5. (신규, D7 후속) 엣지 소스 확장 — 아구몬 fan-out

**What** — 아구몬 성숙기의 fan-out을 vpet(Digital Monster Ver.1)/카드게임 엣지까지
인정해 넓힌다. DWDS 진화표 기준 아구몬 라인은 그레이몬/지오그레이몬/도트아구몬
3갈래가 전부이고 claudemon은 이미 2개(그레이몬·지오그레이몬)를 편입했다. 티라노몬·
시드라몬·다크티라노몬은 DWDS에서 각각 길몬 라인·유키아구몬 라인 소속이라, 이들을
아구몬 성숙기 분기로 넣으려면 DWDS 밖의 vpet/카드게임 엣지를 새 게이트로 인정해야
한다.

**로스터** — 개체는 전부 DWDS 397 로스터 안(#087/#090/#131)이라 도트는 확보 가능.
그레이몬(청)·메라몬은 로스터 부재로 영구 제외.

**Why** — 아구몬 fan-out을 늘리는 유일한 길이다. D2 완화(D7)와 완전히 독립된 결정이라
같은 PR에 섞지 않는다 — 여긴 "어떤 엣지를 정사로 인정할지"의 문제이고 D7은 "미완주
라인을 어떻게 표현할지"의 문제다.

**Depends on** — 없음. 착수 전 결정 필요: vpet/카드게임 엣지를 게이트 통과 기준(§10.1)에
어떻게 반영할지 문서화가 선행돼야 한다(아래 6번).

---

## 6. (신규, D7 후속) 커스텀 팩 계약 문서화 — ✅ 완료 (Phase B README 재작성으로 흡수, 2026-08-20)

**완료** — Phase B 전환으로 README §새 팩 등록하기가 전역 그래프 기반 예시로 재작성됐다. 최소 예시(궁극체 종점 라인)가 `evolution-graph.json` 노드 배열 형태로 제공되고, 종점 판정("다음 칸에서 나를 부모로 지목한 노드가 없음")이 명시됐다. D7의 `terminal: true` 플래그는 Phase B에서 소멸했으므로 그 계약은 더 이상 문서화 대상이 아니다.

---

## 7. (신규, D7 후속) 도트아구몬 / 도트샤인그레이몬 편입 후보

**What** — DWDS 진화표에 `라이즈그레이몬 → 도트샤인그레이몬`이 명시돼 있고
도트아구몬은 로스터 #076이다. 아구몬 팩 +1 루트 후보(Phase 2 — D7 계획서 §2.8이
가오몬 편입만 Phase 1으로 두고 이 라인은 도트/초상 립이 병목인 Phase 2로 미뤘다).

**Why** — 3번 트랙(백로그 도트 추출)과 같은 파이프라인으로 처리 가능한 저비용 후보다.

**Depends on** — 3번의 추출 파이프라인.

---

## 8. (신규, D7 후속) 전역 진화 그래프 — Phase B(그래프/자산 분리) ✅ 완료 → Phase A(지연 진화 완성) ⏸ 대기

**Phase B 완료 (2026-08-20)** — 팩별 트리를 전역 그래프(`evolution-graph.json`, 79노드 / 90→94엣지)로 병합했다. 관계를 자식이 `evolvesFrom: [{from, when}]`으로 소유하고, 런타임이 역인덱스 `childrenOf`에서 후보 풀을 파생한다. 중복 노드 11개 소멸(오메가몬·베르제브몬 등). 종점은 플래그 없이 "자식 0개"로 파생된다. 골든 스냅샷 회귀(3840 케이스 × 7칸 = 26,880 비교)에서 의도된 차이 1종만 허용. **§B-6 편입 4건 완료 (같은 날)**: 궁극체 칸 분기점 0 → 3, 완전체 칸 분기점 5 → 6, 조건부 엣지 비율 14% → 18%, 초궁극체 도달 경로 26 → 32. 도트 0장 추가. 축 배정: dailyUserTurns·outputPerTurn (신규 활용) + topSharePct·sessionCount (기존). **A 범주 첫 실적** (darkdramon ← weregarurumon, 축 충돌 회피 사례). **다음 착수 대상**: §B-6 남은 후속 — 궁극체 17개·유년기 10개의 `Evolves From` 조회 + A 범주 자동 대조 (`audit-canon-edges.js`). 상세는 `docs/global-graph-plan.md` §3·§5·§6·§B-6.

**What(Phase A)** — 로키(digitama) 선택까지 조건화해 지연 진화를 완성한다. 상세 설계는 `docs/global-graph-plan.md` §4.

**데이터 모델 확정 (2026-08-20)** — 관계를 **자식이 `evolvesFrom: [{from, when}]`으로 소유**한다
(계획서 §B-1). 부모가 `evolutions`로 내보내는 지금 방향의 역전이다. 판단 노드는 다음 칸에서
"나를 부모로 지목하고 오늘 조건에 맞는 노드"를 고르므로 **후보 풀이 선언이 아니라 파생**된다
(§B-1.1). 부수 효과로 `terminal` 플래그가 소멸하고(D7 계약 대체) `missing-unconditional-edge`
규칙이 불필요해진다. 조건은 노드가 아니라 `evolvesFrom` 항목마다 붙인다 — 베르제브몬·
블랙래피드몬이 이미 부모별로 조건이 달라서 노드 단위로는 표현이 불가능하다.

**Why** — `pack.json`이 계보 그래프·표시 이름·도트 파일 소유를 겸해 라인 교차가
불가능하고 중복 노드가 생긴다(오메가몬·베르제브몬 등, `docs/global-graph-plan.md` §1).
D7이 D3(`docs/evolution-routes.md` §8)의 보류 근거("D2와 상충")를 없애 지연 진화
재검이 가능해졌다.

**주의** — Q1 선결조건은 **해소됐다**(2026-08-20, `docs/evolution-routes.md` §9 Q1 종결 —
`인펠몬 → 베르제브몬` 유지). 그 검증에서 `베르제브몬 → 아마게몬`이 가짜 엣지로 드러나
제거됐고(§8 D8), 그 김에 **Phase B의 성공 지표 자체가 교체됐다** — "경로 수 26 → 28"은
갈래가 아니라 말단 조합을 세는 지표여서 폐기하고, **분기점(fan-out ≥ 2) 수와 조건부 엣지
비율**(기준선 14개 / 14%)로 다시 세웠다. Phase B의 목표는 "경로 늘리기"가 아니라
**도트 0장으로 분기점 만들기**다. 궁극체 17칸에 분기점이 0개라는 것이 지금 가장 큰 구멍이고,
후속 작업 목록은 `docs/global-graph-plan.md` §B-4·§B-6. ✅ **Phase B 완료로 `terminal: true`
플래그가 제거됐다**(가오몬 미라지가오가몬·케라몬 베르제브몬 두 곳, 종점이 역인덱스에서
파생되는 것으로 대체). 남은 열린 질문은 Q5(로스터 밖 기존 엣지 소급 적용). Phase A는 Phase B
없이는 착수 불가(로키에 조건을 걸 노드 모음 자체가 병합 후에만 존재한다). Phase A는 셔플 덱
로테이션·`mon-history.json`(#23, `81a3f5f`)의 존재 이유를 없앤다 — 되돌릴 수 없는
트레이드오프다(`docs/global-graph-plan.md` §4 A-1).

**Depends on** — Q1 해소 완료 ✅. Phase A는 Phase B 완료 후 — ✅ 착수 가능.

---

## 9. (신규, Phase B 후속) 레이브몬·레이브몬 BM 초상 부재

**What** — `sprites/packs/falcomon/`에 `portrait-ultimate-ravemon-*.png`가 애초에 없다. 프레임(`large-ultimate-ravemon-*.png`)은 있고 초상만 없는 기존 결함이다. 팔코몬 팩의 드롭다운 헤더가 궁극체 이상에서 32px 정수배 확대로 폴백한다.

**Why** — Phase B 전환 자체와 무관하나(레거시에도 이미 없었다), 전역 그래프 병합으로 팔코몬 라인이 초궁극체까지 이어져 가시성이 높아졌다.

**Depends on** — 없음. `scripts/extract_portraits_dwds.py`가 이미 있으므로 시트 확보 후 즉시 편입 가능.

---

## 10. (신규, Phase B 후속) 레거시 접두사 파일 정리 + `pack.json` `tree` 제거

**What** — `sprites/packs/*/adult-0.png` 같은 레거시 스테이지 접두사 파일을 정리하고, 그 후 `pack.json`의 `tree` 필드를 제거한다. 순서는 **런타임/소비자 이관 → 레거시 접두사 파일 정리 → `tree` 제거**다(`docs/global-graph-plan.md` 결정 2번).

**Why** — 런타임과 같은 커밋에서 `tree`를 지우면 도트 파이프라인이 죽는다 — `scripts/sprite_status.py`가 레거시 접두사 파일을 "알려진 레거시"로 분류하는 데 `tree`를 쓴다. 그래서 런타임이 이미 전환됐어도 도트 파이프라인 의존성 때문에 `tree`는 유지 중이다.

**Depends on** — Phase B 완료 ✅. 도트 파이프라인(`sprite_status.py`)의 `tree` 의존성 제거가 선행돼야 한다.
