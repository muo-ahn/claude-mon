# TODOS

의도적으로 미룬 작업. 각 항목은 3개월 뒤에 읽어도 동기와 시작점이 복원되도록 적는다.

> **우선순위 (2026-08-06)**: 3·4번 variation 확장 트랙을 1순위로 승격, 착수. 1·2번은 후순위.
> 착수 시점 실사 결과: §10 백로그 중 블랙워가루몬·블랙라피드몬·블랙세인트가르고몬·쿠즈하몬은
> 이미 트리에 편입돼 있었다(§10이 2026-07-30 기준이라 낡았음 — 당일 실사로 갱신 완료).
>
> **1차 편입 완료 (2026-08-06)**: 임프몬 다크 라인 이블몬→미이라몬→데스몬→데스몬 블랙
> (+1 루트, 게이트 sessionCount ≥ 5). DWDS 시트 4장 신규 확보(Spriters Resource
> 48620/48669/48818/48820), 도트·초상 추출, 트리 편입, `node --test` 70/70 ·
> `sprite_status.py --check` 통과. 상세는 `docs/evolution-routes.md` §10.
>
> **2차 편입 완료 (2026-08-06, PR #18)**: Q2 재검 결과 상당수는 시트 문제가 아니라
> **계보 확인 문제**였다 — Wikimon "Evolves To" 전수 재검으로 길몬 카오스 라인(+1),
> 브이몬 아머체 2종(+2), 테리어몬 프라리몬(+1, 시트 1장 신규)이 기존 에셋으로 해소됐다.
> 하루 루트 합계(전 팩, 맵 진단 기준): 오늘 아침 19 → 1·2차 후 26. 남은 것은 진짜
> 막힌 것뿐(§10 잔여 표): 타이란트캅테리몬 도트,
> 가오몬 BM 대체 시트, 레나몬 분기 소재 — 셋 다 외부 립 발굴 대기. 소서리몬·크리피몬·
> 세이버레오몬은 정사 접점 부재로 사실상 영구 제외.

---

> 맥락: 조건부 진화 엣지는 2026-08-05 `feat/conditional-evolutions` 7커밋으로 구현 완료.
> 분기 선택이 날짜 해시에서 조건 게이트(`evolutions[].when`)로 바뀌었고, 도달 보장은
> "비최상위 노드의 마지막 엣지 = `when: null`" 데이터 계약(`validatePackTree`)이 됐다
> (규칙·근거는 README §랜덤 진화와 해당 커밋 메시지들). 1·2번은 그 설계 리뷰에서 범위
> 밖으로 밀어낸 항목, 3·4번은 후속 variation 확장 트랙이다.

---

## 1. 스테이지 스킵 검증을 팩 스키마 계약으로 승격

**What** — `evolutions[].to` 가 *바로 다음 선언 스테이지*의 노드를 가리켜야 한다는 규칙을
`docs/evolution-routes.md` 의 루트 요구사항으로 명문화한다.

**Why** — 이 제약은 지금 구현 세부에만 존재한다. `lib/daily.js` 의 `byId` 는 전 스테이지
노드를 한 맵에 담고, 후보 필터에 스테이지 검사가 없다. 그래서 성숙기 노드가 궁극체 id를
가리키면 그 노드가 **완전체 슬롯에 배정된다.** 스프라이트 이름 조회가 어긋나 메뉴바 앱이
한 단계 아래 프레임으로 폴백하고(README §스프라이트 폴백), 사용자에게는 "진화했는데
퇴화한 것처럼" 보인다 — README 가 과거 버그로 기록해둔 바로 그 증상이다.
README 는 커스텀 팩 제작을 공개 계약으로 약속하는데, 제작자가 이 제약을 알 방법이 없다.

**Pros** — 커스텀 팩 생태계에서 사일런트 실패를 예방한다.
**Cons** — 조건부 엣지 작업의 `validatePackTree` 가 실용적으로는 이미 잡는다. 남은 건
대부분 문서 작업이다.

**Depends on** — ~~조건부 엣지 변경의 `validatePackTree` 완료 후~~ → 충족됨 (2026-08-05,
`validatePackTree` 가 `node --test` 전수 테스트로 스테이지 스킵을 잡는다. README
§랜덤 진화에도 규칙이 실렸다). 남은 것은 `docs/evolution-routes.md` 루트 요구사항
명문화뿐.

---

## 2. 퇴화(`TREE.regression`) 활성화 검토

**What** — `evolution-tree.json` 에 `regression` 키를 넣어 `applyRegression` 을 살릴지
결정한다.

**Why** — `claudemon-prd.md` §2 의 명시 요구사항이다("방치 또는 나쁜 습관에 따라 조건
저하 → 퇴화 가능"). 코드는 이미 있다 — `lib/evolve.js:66 applyRegression` 이 마지막 활동
시각으로부터 N시간 경과 시 지정 스테이지로 되돌린다. 그런데 `evolution-tree.json` 에
`regression` 키가 없어서 `if (!rule) return false` 로 즉시 반환하는 **죽은 경로**다.

**Pros** — PRD 완결. 방치에 대한 피드백 루프가 생긴다.

**Cons** — 세 가지가 걸린다.
1. `locked` lazy binding(화면 무깜빡임 보장, `lib/daily.js:211-219`)과 정면 충돌한다.
   "조건이 거짓으로 돌아가면 되돌아가나, 아니면 래치인가"를 먼저 정해야 한다.
2. `statusline.js:45` 가 이미 `applyRegression` 을 호출한다. 데이터를 넣는 순간
   statusline 동작이 바뀐다 — 조용한 활성화가 아니다.
3. 하루 KST 리셋 모델에서는 자정 리셋이 이미 사실상의 퇴화다. 중복일 수 있다.

**Context** — 일일 리셋은 의도된 동기 설계라는 결정이 이미 내려져 있다(2026-08-05,
Approach C 기각 근거). 퇴화를 켤 때 그 결정과 충돌하지 않는지 함께 봐야 한다.

**Depends on** — 없음. 단 조건부 엣지 변경 이후가 안전하다.

---

## 3. Variation 확장 트랙 1 — 백로그 도트 추출·편입 (계약 비충돌분)

**What** — `docs/evolution-routes.md` §10 백로그(DWDS 시트 확보 완료 ✅)를 도트로
추출해 팩 트리에 `evolutions` 노드로 편입한다. 순서:

1. 시트별로 `python3 scripts/extract_pack_evolved_dwds.py --contact <sheet>` 를 돌려
   번호 스트립을 **눈으로 보고** 정면 포즈 프레임 인덱스 2개를 고른다.
   **인덱스 추측 금지** — 재실행이 현재 파일을 덮어쓰고 되돌릴 근거가 없다
   (스크립트 주석의 명시 경고. `PICKS` 에 등록된 것만 추출된다).
2. `PICKS` 등록 → 추출 → `docs/sprite-aliases.yaml` / `sprite_status.py --check` 정합.
3. `pack.json` 에 조건 엣지로 편입. **비최상위 노드의 마지막 엣지 = `when: null`
   계약**(README §랜덤 진화)을 지켜야 하며, `node --test` 의 출하 팩 전수 검증이 안전망이다.

**Why** — 조건부 진화 게이트는 깔렸는데 정작 분기가 있는 팩이 10개 중 6개뿐이다
(길몬·임프몬·레나몬·가오몬은 단일 루트 — 조건이 걸릴 자리가 없다). 조건 게이트의
체감 가치는 분기 수에 비례한다.

**주의** — 백로그 노드 다수(테리어몬 블랙라피드몬 계열, 임프몬 +7, 레나몬 쿠즈하몬)는
편입 후 라인이 **초궁극체까지 이어지는지**가 관건이다. 이어지지 않는 노드는 4번(Q2)이
풀리기 전까지 편입 불가 — 파피몬 블랙워가루몬(D4, 다크드라몬으로 이어짐)처럼 기존
루트를 정상화하는 것부터 먹는다.

**Depends on** — 없음 (사람이 인덱스를 고르는 수작업이 병목).

---

## 4. Variation 확장 트랙 2 — Q2 해소: 궁극체에서 멈추는 정사 라인 되살리기

**What** — 정사상 초궁극체가 없어 걷는 트리에서 빠진 라인을 되살릴 방법을 결정한다.
대상: 길몬 블랙메가로그라우몬→카오스듀크몬, 브이몬 화염드라몬·매그너몬(아머체),
그리고 3번 백로그 중 궁극체에서 끝나는 노드 전부. 제외 이력과 재편입 대상은
`docs/sprite-aliases.yaml` `pack_sprite_to_catalog` 의 2026-08-05 주석에 있다.

**옵션 (evolution-routes.md Q2):**

- **(a) 도달 보장 계약 완화** — 멈추는 라인을 조건부로 허용(지연 진화 A안 계열).
  2026-08-05 에 락인한 결정 C("마지막 엣지 = `when: null`")를 다시 여는 것이라
  `validatePackTree`·전수 테스트·README 계약·맵 진단이 연쇄로 바뀐다. **비추천** —
  "2M 찍은 날 초궁극체를 못 보는" 사용자 경험 회귀(D2 의 존재 이유)가 되살아난다.
- **(c) 초궁극체 도트를 다른 출처에서 확보** — Lost Evolution 립 등에서 해당 라인의
  초궁극체 도트를 구해 라인을 끝까지 잇는다. 계약·코드 무변경, 스프라이트 작업만.
  **권장.** 가오몬(궁극체 시트 부재로 로테이션 제외 중)의 로테이션 합류도 같은
  작업의 부산물로 얻는다.

**Why** — 3번만으로는 게임 기준 variation 의 절반이 잠긴 채로 남는다. 백로그 표의
"Q2 해결 시 +N" 루트 증가분(테리어몬 +2, 임프몬 +3, 레나몬 +1)이 전부 여기 묶여 있다.

**Depends on** — 3번과 독립이나, (c)안 채택 시 3번의 추출 파이프라인을 그대로 쓴다.
(a)안을 진지하게 검토하려면 결정 C(도달 보장을 데이터 계약으로)의 락인 근거 — README
§랜덤 진화, `feat(daily): 분기 선택을 날짜 해시에서 조건 게이트로` 커밋 본문,
`docs/evolution-routes.md` D2/D3 — 를 먼저 읽을 것.
